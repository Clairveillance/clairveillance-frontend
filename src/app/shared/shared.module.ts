import { NgModule } from '@angular/core';
import { modules as fromModules } from './shared.modules.imports';
@NgModule({
  declarations: [],
  imports: [...fromModules],
})
export class SharedModule {}
